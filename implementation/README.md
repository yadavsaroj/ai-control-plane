# Implementation

This directory contains the reference implementation for the AI Control Plane.

The implementation should stay aligned with the architecture notes and architecture decision records.

Initial areas:

- `gateway/` for AI gateway services.
- `sdk/` for developer-facing integration libraries.
- `observability/` for telemetry and operational signals.
- `policy-engine/` for policy evaluation and enforcement.
